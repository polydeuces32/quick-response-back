# Copy to .env.local and adjust. Required for Prisma + API routes.
# Path is relative to the prisma/ folder (see Prisma SQLite docs).
DATABASE_URL="file:./dev.db"

# Required for AI Mode (claude-cookbooks integration via Anthropic SDK)
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."
